version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
artifacts:
  files:
     - './Device_Farm-POC-Repo/app/build/outputs/**/*.apk'
  discard-paths: yes

